{
  "name": "openclaw-sequence-dashboard-plugin",
  "version": "0.1.0",
  "description": "OpenClaw plugin: local runtime console with overview and sequence replay for multi-agent execution.",
  "private": false,
  "license": "MIT",
  "type": "commonjs",
  "main": "index.js",
  "files": [
    "index.js",
    "openclaw.plugin.json",
    "dashboard",
    "examples",
    "scripts/configure-openclaw.js"
  ],
  "engines": {
    "node": ">=18"
  },
  "openclaw": {
    "extensions": [
      "./index.js"
    ]
  },
  "scripts": {
    "build:ui": "npm --prefix dashboard-ui run build",
    "test": "node --test tests/*.test.js",
    "check": "node --check index.js && node --check dashboard/live-dashboard-server.js && node --check scripts/configure-openclaw.js && npm --prefix dashboard-ui run build && node --test tests/*.test.js",
    "pack:local": "npm pack"
  },
  "keywords": [
    "openclaw",
    "plugin",
    "dashboard",
    "timeline",
    "a2a",
    "sessions_spawn"
  ]
}
